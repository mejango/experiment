name: contract_user

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  name_user:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/properties/name_user
  key:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/key
  contract:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/abstract/contract

dev_dependencies:
  flutter_test:
    sdk: flutter
