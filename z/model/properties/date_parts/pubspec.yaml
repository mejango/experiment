name: date_parts

version: 1.0.0+1

environment:
  sdk: ^3.7.2

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  mappable:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: z/model/mixins/mappable
  key:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: z/model/key

dev_dependencies:
  flutter_test:
    sdk: flutter
