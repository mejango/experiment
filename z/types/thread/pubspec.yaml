name: thread_type
version: 1.0.0+1
environment:
  sdk: ^3.7.2

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  string_from_enum_string:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: z/lib/string_from_enum_string

dev_dependencies:
  flutter_test:
    sdk: flutter
