name: keyboard_accessory
description: The component and mechanism for providing an accessory view on a keyboard.

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
