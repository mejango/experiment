name: theme
description: Theme definitions.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  color:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: spec/color

dev_dependencies:
  flutter_test:
    sdk: flutter
