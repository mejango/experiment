name: name_context

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  context_type:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: types/context
  mappable:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/mixins/mappable
  key:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/key

dev_dependencies:
  flutter_test:
    sdk: flutter
