name: settlement

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  one_to_one_transaction:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/abstract/one_to_one_transaction

dev_dependencies:
  flutter_test:
    sdk: flutter
