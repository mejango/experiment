name: public_completion

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  key:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: model/key
  data_chunk:
    path: ../../data_chunk

dev_dependencies:
  flutter_test:
    sdk: flutter
