name: commands
description: Library for issuing commands via blossm
version: 1.0.0+1
environment:
  sdk: '>=2.2.2'
dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+3
  device_info: ^0.4.0+1
  blossm_command:
  secure_storage:
  navigator:
  welcome_artboard:
  auth_verify_code_artboard:
dev_dependencies:
  flutter_test:
    sdk: flutter
