name: join_service_artboard
description: Allows the user to join a service.
version: 1.0.0+1
environment:
  sdk: '>=2.1.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  form_vertical_floating_artboard_template:
  form_builder:
  app_data:
  navigator:
  commands:
  services_artboard:
dev_dependencies:
  flutter_test:
    sdk: flutter
