name: auth_verify_code_artboard
description: Prompts the user to verify a security code
version: 1.0.0+1
environment:
  sdk: '>=2.1.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  form_vertical_floating_artboard_template:
  form_builder:
  app_data:
  navigator:
  commands:
  services_artboard:
dev_dependencies:
  flutter_test:
    sdk: flutter
