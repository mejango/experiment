name: challenge_artboard
description: Allows a user to confirm their account by inputing a code sent to them.
version: 1.0.0+1
environment:
  sdk: '>=2.0.0-dev.68.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  form_vertical_floating_artboard_template:
  form_builder:
  app_data:
dev_dependencies:
  flutter_test:
    sdk: flutter
