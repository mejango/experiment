name: sign_up_artboard
description: Allows the user to create an account.
version: 1.0.0+1
environment:
  sdk: '>=2.0.0-dev.68.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  form_vertical_floating_artboard_template:
  form_builder:
  app_data:
  challenge_artboard:
  phone_number_sign_up_artboard:
  navigator:
dev_dependencies:
  flutter_test:
    sdk: flutter
