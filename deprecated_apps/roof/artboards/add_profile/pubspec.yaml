name: add_profile_artboard
version: 1.0.0+1
environment:
  sdk: '>=2.1.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  spaced_column_vertical_floating_artboard_template:
  app_data:
  button_builder:
  navigator:
  spaced_column_builder:
dev_dependencies:
  flutter_test:
    sdk: flutter
