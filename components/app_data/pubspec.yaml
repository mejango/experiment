name: app_data
description: An interface to interact with app data.
version: 1.0.0+1
environment:
  sdk: ^3.7.2
resolution: workspace
publish_to: none
dependencies:
  flutter:
    sdk: flutter
  device_info: ^2.0.3
  network:
  synchronizer:
  session:
  public:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
