import 'package:flutter/material.dart';
import 'package:distance/index.dart' as distance;

const field1 = EdgeInsets.symmetric(vertical: distance.d);

const field2 = EdgeInsets.symmetric(vertical: distance.c);

const container1 = EdgeInsets.all(distance.c);

const section1 =
    EdgeInsets.fromLTRB(distance.c, distance.b, distance.c, distance.b);
