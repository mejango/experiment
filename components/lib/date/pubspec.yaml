name: date
description: Date parsing and formatting.

version: 1.0.0+1

environment:
  sdk: ^3.7.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  intl: 0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
