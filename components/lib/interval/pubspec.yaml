name: interval
description: Creates strings from interval values.
version: 1.0.0+1
environment:
  sdk: ^3.7.2
resolution: workspace
publish_to: none
dependencies:
  flutter:
    sdk: flutter
  period_type:
  ordinal_suffix_string_from_int:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
