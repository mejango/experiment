name: interval
description: Creates strings from interval values.

version: 1.0.0+1

environment:
  sdk: ^3.7.2

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  period_type:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: components/types/period
  ordinal_suffix_string_from_int:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: components/lib/ordinal_suffix_string_from_int
  

dev_dependencies:
  flutter_test:
    sdk: flutter
