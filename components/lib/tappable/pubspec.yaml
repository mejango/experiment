name: tappable
description: Supplies custom GestureDetector builder method and tapped state tools.

version: 1.0.0+1

environment:
  sdk: ^3.7.2

resolution: workspace
  
dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
