name: metadata
version: 1.0.0+1
environment:
  sdk: ^3.7.2
resolution: workspace
publish_to: none
dependencies:
  flutter:
    sdk: flutter
  event_meta_value_type:
  mappable:
  key:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
