name: frequency_type
version: 1.0.0+1
environment:
  sdk: ^3.7.2

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  period_type:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: components/types/period
  labeled_value:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: components/labeled_value

dev_dependencies:
  flutter_test:
    sdk: flutter
