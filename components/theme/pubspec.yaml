name: theme
description: Theme definitions.

version: 1.0.0+1

environment:
  sdk: ^3.7.2

resolution: workspace

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  color:
    git:
      url: git@github.com:mejango/experiment.git
      ref: main
      path: components/spec/color

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
