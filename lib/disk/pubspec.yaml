name: disk
description: Read and write to disk.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0"
  
dependencies:
  flutter:
    sdk: flutter
  path_provider: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
