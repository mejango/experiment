name: cache
description: A lightweight cache.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^0.5.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
