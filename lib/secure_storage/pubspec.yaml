name: secure_storage
description: Encrypted storage for sensitive data.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
