name: ordinal_suffix_string_from_int
description: A function to transform an int into a string with an ordinal suffix (3 => 3rd).

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
