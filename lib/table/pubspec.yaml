name: table
description: Roof's table tools.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stream:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: lib/stream

dev_dependencies:
  flutter_test:
    sdk: flutter
