name: synchronizer
description: A tool to parse data recieved by the server.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  network:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: lib/network

dev_dependencies:
  flutter_test:
    sdk: flutter
