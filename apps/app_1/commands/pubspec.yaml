name: commands
description: Library for issuing commands via blossm
version: 1.0.0+1
environment:
  sdk: ^3.7.2
resolution: workspace
publish_to: none
dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  device_info: ^2.0.3
  blossm_command:
  secure_storage:
  navigator:
dev_dependencies:
  flutter_test:
    sdk: flutter
