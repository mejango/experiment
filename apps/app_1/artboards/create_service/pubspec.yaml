name: create_service_artboard
description: Allows the user to create new service.
version: 1.0.0+1
environment:
  sdk: ^3.7.2
resolution: workspace
publish_to: none
dependencies:
  flutter:
    sdk: flutter
  form_vertical_floating_artboard_template:
  form_builder:
  app_data:
  navigator:
  commands:
  form_validation_exception:
  form_submission_exception:
dev_dependencies:
  flutter_test:
    sdk: flutter
