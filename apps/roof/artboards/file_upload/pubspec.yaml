name: file_upload_artboards
description: The artboards used to upload files via camera or gallery.
version: 1.0.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  navigator:
    git:
      url: git@github.com:RoofVillage/roof-x.git
      ref: master
      path: lib/navigator
  image_picker: ^0.5.0+6

dev_dependencies:
  flutter_test:
    sdk: flutter
